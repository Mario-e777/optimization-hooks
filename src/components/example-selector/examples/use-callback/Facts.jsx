export const FACTS = [
    <>👉🏻 Cache the function definition between re-renders.</>,
    <>👉🏻 <code>Callback function</code> takes arguments, like <code>{'(args) =>'}</code>, and returns a callback with the same definition every re-render.</>,
    <>👉🏻 Save the initial parent values </>,
    <>👉🏻 Dependencies are compared with <code>Object.is</code></>,
    <>👉🏻 Try <code>useCallback</code> as an experiment to verify whether the total logged time has decreased for that interaction or not.</>,
    <>👉🏻 If a specific interaction feels laggy, use the React Developer Tools profiler to see which components would benefit the most from memoization, and add memoization where needed.</>,
];
