export const FACTS = [
    <>👉🏻 Cache the function definition between re-renders.</>,
    <>👉🏻 <code>Callback function</code> takes arguments, like <code>{'(args) =>'}</code>, and returns a callback with the same definition every re-render.</>,
    <>👉🏻 Try <code>useEvent</code> as an experiment to verify whether the total logged time has decreased for that interaction or not.</>,
    <>👉🏻 Keep in mind that your machine is probably faster than your users’.</>,
    <>👉🏻 If a specific memoized component feels laggy, use the React Developer Tools profiler to see if the component would benefit the most from <code>useEvent</code>.</>,
];
